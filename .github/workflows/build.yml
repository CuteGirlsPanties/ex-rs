name: Build and release

on:
  push:
    branches: [main]
  release:
    types: [created]

jobs:
  # build:
  #   strategy:
  #     matrix:
  #       include:
  #         - os: ubuntu-22.04
  #           art: ex_linux-amd64
  #         - os: macos-13
  #           art: ex_macos-amd64
  #         - os: macos-14
  #           art: ex_macos-arm64
  #         - os: windows-2022
  #           art: ex_windows-amd64
  #   runs-on: ${{ matrix.os }}
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: cargo build --release
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: Art
  #         path: target/release/ex-rs(.exe)?
  
  # release:
  #   runs-on: ubintu-22.04
  #   steps:

  xxx:
    strategy:
      matrix:
        include:
          - os: ubuntu-22.04
            art-name: ex_linux-x86_64
            art-path: target/release/ex-rs
          - os: macos-13
            art-name: ex_macos-x86_64
            art-path: target/release/ex-rs
          - os: macos-14
            art-name: ex_macos-aarch64
            art-path: target/release/ex-rs
          - os: windows-2022
            art-name: ex_windows-x86_64
            art-path: target/release/ex-rs.exe
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: |
          cargo build --release
          ${{ matrix.art-path }}
